{% set name = "georaster" %}
{% set version = "1.24" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: cb7bf64d491d2336be946d89003ba84866e379f9f2a0b016953fed1cfdfa3302

build:
    number: 0
    script: python setup.py install

requirements:
  build:
    - python

  run:
    - python
    - numpy
    - scipy
    - gdal
    - pyproj

test:
  imports:
    - georaster

about:
  home: https://github.com/GeoUtils/georaster
  license: GNU GPL v.3
  summary: 'easy processing and analysis of geographic and projected rasters'
  license_family: GPL3
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE.txt'
  doc_url: http://georaster.readthedocs.io
  dev_url: https://github.com/GeoUtils/georaster

extra:
  recipe-maintainers:
    - atedstone
